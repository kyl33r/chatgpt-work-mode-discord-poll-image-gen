{
  "name": "chatgpt-work-mode-discord-poll-image-gen",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "configure:channel": "tsx src/configure-channel.ts",
    "round": "tsx src/cli.ts",
    "migrate:state": "tsx src/migrate-state.ts",
    "migrate:channel-allowlist": "tsx src/migrate-channel-allowlist.ts",
    "openclaw:profile": "tsx scripts/openclaw-profile.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "validate:skills": "tsx scripts/validate-skills.ts",
    "verify": "npm test && npm run build && npm run validate:skills"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "openclaw": "2026.8.1",
    "tsx": "^4.23.12",
    "typebox": "1.3.16",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "sharp": "^0.35.3"
  }
}
